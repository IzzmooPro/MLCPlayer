# MLC Player - bağımlılıklar
# Kurulum: pip install -r requirements.txt
PyQt6>=6.10.2
python-mpv>=1.0.8
# Guncelleme imzasi (Ed25519) dogrulamasi icin. Kendi kripto kodumuzu
# yazmiyoruz; bkz. app/release_signature.py.
cryptography>=45.0
# NOT: Python `yt-dlp` paketi ARTIK ürün bağımlılığı DEĞİLDİR.
# mpv site çıkarımını HARİCİ bir çalıştırılabilir üzerinden yapar; MLC Player
# resmî `bin/yt-dlp.exe` ve `bin/deno.exe` dosyalarını kendi kurulumunda
# taşır (bkz. bin/RUNTIME_MANIFEST.txt ve docs/PACKAGING_PLAN.md).
# Kullanıcıdan `pip install yt-dlp` İSTENMEZ.
